/**
 * Default render for the `@modal` parallel slot: nothing. Intercepting routes
 * fill the slot; otherwise (and on hard navigation) it stays empty.
 */
export default function ModalDefault() {
  return null;
}
